import React from 'react';
import { Field, reduxForm } from 'redux-form';

export const Register = (props) => {
  const { handle, pristine, reset, submitting } = props;
  return (
    <Field
      name="username"
      c
    />
  );
};
